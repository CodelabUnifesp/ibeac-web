import {Stack} from '@chakra-ui/layout';
import styled from 'styled-components';

export const StackAside = styled(Stack)`
  position: -webkit-sticky;
  position: sticky;
  top: 25px;
`;

export default {
  StackAside,
};
